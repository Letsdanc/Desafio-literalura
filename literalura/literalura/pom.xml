<project xmlns="http://maven.apache.org/POM/4.0.0" 
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.alura</groupId>
    <artifactId>literalura</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>literalura</name>
    <description>Aplicación de consola con Spring Boot y PostgreSQL</description>

    <properties>
        <java.version>17</java.version> <!-- Configura la versión de Java -->
        <spring-boot.version>3.2.4</spring-boot.version> <!-- Usa la última versión de Spring Boot -->
    </properties>

   <dependencies>
    <!-- Dependencia de Spring Boot Starter Web -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
        <version>3.2.4</version> <!-- Agregar la versión aquí -->
    </dependency>

    <!-- Dependencia de Spring Boot Starter Data JPA -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-jpa</artifactId>
        <version>3.2.4</version> <!-- Agregar la versión aquí -->
    </dependency>

    <!-- Dependencia de PostgreSQL -->
    <dependency>
        <groupId>org.postgresql</groupId>
        <artifactId>postgresql</artifactId>
        <version>42.6.0</version> <!-- Agregar la versión aquí -->
    </dependency>

    <!-- Dependencia de Jackson para JSON -->
    <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
        <version>2.15.1</version> <!-- Agregar la versión aquí -->
    </dependency>

    <!-- Dependencia para Spring Boot Starter Test -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-test</artifactId>
        <version>3.2.4</version> <!-- Agregar la versión aquí -->
        <scope>test</scope>
    </dependency>
</dependencies>
<build>
    
    <plugins>
        <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>exec-maven-plugin</artifactId>
            <version>3.1.0</version>
            <configuration>
                <mainClass>com.alura.literalura.LiteraluraApplication</mainClass>
            </configuration>
        </plugin>
    </plugins>
</build>



</project>
